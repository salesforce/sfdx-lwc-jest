import { Element, api } from 'engine';

export default class Formatted-name extends Element {
    @api firstName
	@api format
	@api informalName
	@api lastName
	@api middleName
	@api salutation
	@api suffix
    
}