import { Element, api } from 'engine';

export default class RecordForm extends Element {
    @api columns
	@api fields
	@api layoutType
	@api mode
	@api objectApiName
	@api recordId
	@api recordTypeId
    
}