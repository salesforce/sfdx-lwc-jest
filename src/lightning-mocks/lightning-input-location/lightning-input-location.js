import { Element, api } from 'engine';

export default class Input-location extends Element {
    @api disabled
	@api label
	@api latitude
	@api longitude
	@api readonly
	@api required
	@api variant
    
}